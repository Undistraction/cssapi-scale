export const CONFIG_ARG_NAMES = Object.freeze({
  CONFIG: `config`,
})

export const ERROR_PREFIX = `[cssapi-scale]`
export const CONFIGURE_PREFIX = `configure()`
export const API_STEP_PREFIX = `step()`
